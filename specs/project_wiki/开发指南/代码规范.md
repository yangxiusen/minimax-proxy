# 代码规范

## 通用约定

- 文件编码统一为 UTF-8，使用所选语言的官方格式化工具。
- 中文注释说明业务意图、映射规则和异常原因，不重复代码表面行为。
- 模块按 API、任务、队列、上游适配、存储和配置职责拆分。
- 关键代理链路记录结构化日志：请求 ID、任务 ID、上游目标、耗时、状态与脱敏错误。
- 禁止记录 Bearer Token、完整敏感请求或本地文件路径。
- `docs/` 仅作为外部资料，不得在运行时代码中直接引用。

