# 监控控制台测试与验收

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 目标版本 | `version_0.0.1` |
| 变更编号 | `001-monitor-console` |
| 测试日期 | 2026-08-07（自动化与本地页面） |
| 测试负责人 | 待人工填写 |

## 2. 自动化用例

| ID | 场景 | 预期结果 |
| --- | --- | --- |
| TC-001 | 正确/错误管理凭据登录和退出 | 正确建立/销毁会话，错误返回 401 |
| TC-002 | 会话过期、伪造 Cookie、重启 | 管理 API 返回 401，不泄露数据 |
| TC-003 | 快照并发读写及字段缺失 | 无竞态；缺失字段为 `null/unknown` |
| TC-004 | 节点健康、异常、恢复 | 状态和时间正确转换，保留脱敏错误 |
| TC-005 | 管理任务筛选和 10/20 分页 | 总数、排序、状态、实例和搜索正确 |
| TC-006 | 当前任务和最近完成任务 | 每个节点最多各返回一条正确记录 |
| TC-007 | 全部节点异常或未知时创建 | 返回 503，数据库不新增任务 |
| TC-008 | 至少一个健康但忙碌节点时创建 | 任务正常入队 |
| TC-009 | V2 原有查询、列表和删除 | 响应契约保持兼容 |
| TC-010 | 页面资源嵌入与无缓存头 | 无外部依赖，受保护响应为 `no-store` |

## 3. 页面验收

- [x] 全部任务与私有服务使用两个清晰独立的区域
- [x] 全部任务默认 10 条，可切换 20 条并正确翻页
- [x] 状态、实例、任务 ID/客户筛选可组合使用
- [x] 节点区域保留私有队列、当前任务和最近一条已结束任务
- [x] 健康、异常、未知、加载、空数据和缓存过期状态清晰
- [x] 常用桌面宽度及窄屏下无重叠、截断或不可操作控件

## 4. 人工确认项

- [ ] 使用真实私有服务验证 CPU、内存、GPU、显存和队列解析
- [ ] 断开并恢复私有服务，确认 5 秒内状态更新及错误脱敏
- [ ] 验证私有服务忙碌时仍可排队、全部断开时返回资源不足
- [ ] 使用生产反向代理验证 HTTPS Cookie、退出和会话过期
- [ ] 管理员确认页面数据足以完成日常只读排查

## 5. 回归与结论

- [x] `go test ./...`
- [x] `go vet ./...`
- [ ] Docker 镜像构建成功
- [x] 原有四个 V2 API 自动化回归通过
- [ ] 真实上游人工联调通过

验收结论：实现、自动化和本地页面验收通过；Docker 构建因本机引擎未启动而未执行，真实私有服务、生产反向代理和人工业务验收待完成。自动化通过不能替代真实私有服务联调。
